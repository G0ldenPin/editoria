name: Pubblicazione Automatica da Issue Approvata

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  publish-from-issue:
    name: Conversione e Pubblicazione del Termine Approvato
    if: contains(fromJSON('["approved", "Approved", "approvato", "Approvato"]'), github.event.label.name)
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout del Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurazione di Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installazione Dipendenze
        run: |
          pip install pyyaml

      - name: Estrazione e Generazione della Scheda Termine
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          python scripts/issue_to_term.py

      - name: Validazione Formale e Semantica del Corpus
        run: |
          python scripts/validate_terms.py

      - name: Compilazione Artefatti del Tesauro (JSON e HTML)
        run: |
          python scripts/build_thesaurus.py

      - name: Commit e Push delle modifiche su main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/terms/ DECISIONS.md dist/
          git commit -m "feat(thesaurus): publish term from approved issue #${{ github.event.issue.number }}" || echo "Nessuna modifica da committare"
          git push

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload Artefatto Statico
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist'

      - name: Deploy su GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Notifica e Chiusura della Issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "🎉 **Termine Approvato e Pubblicato con Successo!**\n\nIl Chief Editor ha approvato la proposta applicando l'etichetta `" + context.payload.label.name + "`.\nLa pipeline automatica ha:\n1. Estratto i metadati e generato la scheda Markdown/YAML in `data/terms/`;\n2. Superato i test di validazione sintattica e ontologica (`validate_terms.py`);\n3. Registrato la delibera in `DECISIONS.md`;\n4. Rigenerato il dataset JSON e distribuito il portale aggiornato su GitHub Pages: https://g0ldenpin.github.io/editoria/"
            });
            await github.rest.issues.update({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'closed'
            });
